# SupportFlow v2 runs on the Python standard library alone.
# Nothing here is required to complete any lab.
#
# ipywidgets ships with Google Colab and powers the Governance Console.
# If it is missing the Console falls back to a text interface.
ipywidgets>=8.0
